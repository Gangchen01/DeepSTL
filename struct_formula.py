#!/usr/local/bin/python3

from binary_tree import Tree
from fractions import Fraction
import numpy as np

class Formula:
    def __init__(self, rules = None, time = None, scale =None, width = None, structure = None):
        self.rules = rules
        self.time = time
        self.scale = scale
        self.width = width
        self.structure = structure

    def Init_Formula(self,time_slices):
        formulas = list()
        return formulas

    def decode_formula(self):
        tree = self.tree
        return tree
    def combine_formula(self, formual_pre, formula_post, method):
        formulas = np.array([[0]])

        return formulas




